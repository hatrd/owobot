# 智能钓鱼 tracer bullet — 2026-09-19

最终代码 `62f36cb` 的独立轮次已成功：一次目标调用后，在 **277.5 秒**内新钓获并保留 **8 条生鳕鱼/鲑鱼**，自动回到原家 `(-1361.5, 69, 1347.5)`，生命/饱食 **20/20**。启动后只做只读观察，没有外部移动、抛竿、恢复或背包操作指令。

机器证据：[fishing-2026-09-19.json](fishing-2026-09-19.json)。目标 ID `5980d7a0-1d1e-4397-a12d-1e6dfa5f8e15`；世界 `40d030d2c1ff2bdfc887efcb`。

| 项目 | 已确认结果 |
| --- | --- |
| 原有生鱼基线 | 9 条，不计入本轮钓获 |
| 本轮新增 | 8 条，10 次正常收竿回执；非目标物品不计数 |
| 背包管理 | 2 次储物箱处理，生鱼保留 |
| 夜间行为 | 自动返家，1 次确认入睡/醒来；之后自动完成目标 |
| 最终库存 | 生鳕鱼 11、生鲑鱼 6；钻石仍为 64 |
| 最终位置 | 原家 `(-1361.5, 69, 1347.5)` |
| 终态 | `succeeded`，`caught=8`，`retainedRaw=8` |
| 资源释放 | `operation=null`，控制租约 `null` |

独立轮次包含一次昼夜边界上的 `sleep_rejected / bot is not sleeping`，已记录原始诊断；本地运行时自行处理后完成，没有人工续跑。事件中没有实际受伤；受伤优先级、立即中断收竿和恢复条件经过自动化测试，不声称做过真实挨打测试，也没有故意伤害 bot。

## 补给和取消验证的边界

开发轮次从没有鱼竿、仅 3 个空位开始。服务自行存入普通挖矿杂物、查找储物箱、找到鱼竿、跨水前往补给区、选择钓点，最终带回首批 8 条生鱼。该轮经历代码修复、扩大搜索范围与 resume，**不计作无干预验收**。

另一次真实取消测试在 `phase=cast / operation=cast` 时调用 cancel，确认收竿退出、操作与租约清空，鱼数仍为 8。随后校准产生另 1 条鱼，所以最终独立轮次以 9 条为基线；该轮从岸边启动，显式指定原家，未冒称从家中空背包出发。

## 工程验收

- 修改文件语法检查通过；新代码重启/热重载上线后检查 tool/observe schema。
- `interaction:dry`、fishing start/resume/cancel 定向 dry、fishing/journey 只读 dry 通过。
- 项目测试：329 通过、1 跳过、0 失败（330 项）。
- Pathfinder fork：lint 与 97 个测试通过；空目录 `npm ci` 成功，所有 prismarine-item 实例统一到固定 fork 提交。
- 冷启动世界身份修复经真实重启确认，原家的保护记录和钻石保留记录重新正确加载。
- 上游 PR 与集成提交详见 [fork 依赖](../dependencies/forks.md)。

使用：`node scripts/fish.js start --count=8`；`status` 返回紧凑进度，`status --detail` 查看证据；`cancel` / `resume` 控制目标。烤鱼不是本目标的完成条件，所有目标鱼保留为生鱼供喂猫。
