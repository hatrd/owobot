# 整理战利品：两轮独立 Codex 实跑

两轮均使用独立 `codex exec -m gpt-6-astra -c 'model_reasoning_effort="low"' --ephemeral --json`，工作目录为新的临时目录，无开发对话历史。提示只提供任务、授权与 `docs/stash.md` 的入口；未向运行中的模型追加提示或代替它操作。各轮命令、模型用量、完整终态与逐笔回执见 [机器证据](stash-2026-09-19.json)。

| 验收 | 首轮冷启动 | 调整后的第二轮 |
| --- | --- | --- |
| 高层 CLI 调用 | 15 | 4 |
| 实际归仓 | 242 件 / 9 笔 | 21 件 / 3 笔 |
| 终态 | succeeded | succeeded |
| 错误 / 未决转移 / 剩余战利品 | 无 | 无 |
| 返回起点误差 | 约 0.34 格 | 约 0.26 格 |
| 生命 / 饥饿 | 20 / 20 | 20 / 20 |
| 保留钻石 / 鳕鱼 / 鲑鱼 | 64 / 11 / 6 | 64 / 11 / 6 |

首轮从真实的几乎满背包开始，空位由 2 增至 13。附近四个潜影盒没有归仓依据且容量有限，工具诚实返回 partial；模型自行将范围从 16 扩大到 32、64，选择实测 18 个空位、无展示框标签的箱子 `(-1315,65,1384)` 作为长期溢出箱，再 resume 完成。存入：干海带块104、金胡萝卜119、经验瓶1、粗铜5、铜矿石2、金矿石1、深层铜矿石5、深层铁矿石3、橡木告示牌2。九笔转移均验证背包减少量等于目标箱增加量。

首轮暴露的交互成本已调整：默认搜索范围64；preview 默认沿用上次任务范围；partial 回执提供计算过容量的直接可执行 configure 建议；根据显式路线、展示框和历史同类物品优先检查相关容器，现场容量足够即停止扫描。展示框在走近后和存入前重新读取，避免远处/高度不同的标签漏读；存储路线与观测按维度隔离。

第二轮是新的模型进程，沿用首轮留下的长期配置和存储记忆。通过公开控制面从附近箱子取出石头8、石英块12、拴绳1并回到起点，作为明确的实物回放样本。它仅调用 `preview`、`run`、`status`、`status --detail`：石头和石英块按已有同类归回附近箱子，拴绳通过已配置的溢出箱归仓，约98秒完成，扫描3处而非遍历全部21个候选；无需模型重新决定目的地或导航节点。已有库存证据与现场变化由执行层重新检查。

这不是同一初始条件下的性能对照：第二轮物品更少，而且已有长期配置。它验证的是后续接入者能否复用记忆，用少量高层指令处理实际物品，而不是空背包 no-op。测试准备曾尝试直接导航到远处仓库，底层 goto 返回 navigation_no_path、未发生取物；随后改用附近箱子完成准备。两轮外部任务本身均由共享的陆地/水面旅程执行器完成返程。

最终全量测试 348 项：347 通过、1 跳过；整理专项 18 项全部通过，interaction:dry 的15项检查全部通过。验证包含语法检查、热重载、schema、完整 interaction:dry、定向只读 preview 与参数 dry。专项测试覆盖保留规则、混合物品容量、双箱、展示框现场变化、跨维度、歧义转移、取消/受伤优先级，以及热重载时未结束的转移结算与恢复。保留现代组件物品是有意的保守边界：底层 deposit 尚不能可靠地按组件选择同名变体。
